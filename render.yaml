services:
  - type: web
    name: culinary-index
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --workers 2 --worker-class gthread --threads 8 --bind 0.0.0.0:$PORT"
